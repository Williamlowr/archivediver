{
  "name": "archivediver",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "make dev",
    "test": "make test",
    "lint": "make lint",
    "format": "make format",
    "dev:web": "npm run dev --prefix apps/web",
    "test:web": "npm run test --prefix apps/web"
  }
}
